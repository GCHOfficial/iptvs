# Copy to .env.local for `npm run dev`, or set as CI variables for the build.
# These are the anon/publishable values — safe to expose. Never use service_role.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-OR-PUBLISHABLE-KEY
